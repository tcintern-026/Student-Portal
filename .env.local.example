# Copy this file to .env.local (Next.js loads .env.local automatically, and
# .gitignore already excludes it from commits).

# Base URL of the Express API, including the /api prefix.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
